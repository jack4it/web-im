workspace:
  base: /data/apps/opt
  path: web-im

pipeline:
  clone:
    image: plugins/git
    environment:
      - DOCKER_LAUNCH_DEBUG=true
    debug: true
    commands:
      - echo 'clone success2'
    volumes:
      - /data/apps/opt/web-im/${DRONE_BRANCH}:/data/apps/opt/web-im
    when:
      branch: drone




